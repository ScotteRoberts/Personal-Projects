// import * as types from "../mutation-types";

let moment = require("moment");

const state = {
  now: moment().format("HHmm")
};

const mutations = {};

const actions = {};

const getters = {};

export default {
  state,
  mutations,
  actions,
  getters
};
